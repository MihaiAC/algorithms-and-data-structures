[project]
name = "lc-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "line-profiler>=5.0.2",
    "pytest>=9.0.2",
    "sortedcontainers>=2.4.0",
    "tqdm>=4.67.3",
]

[tool.uv]
package = true

[tool.setuptools]
py-modules = ["LC_Python.SimpleProfiler"]

[dependency-groups]
dev = [
    "ruff>=0.15.5",
]
