msg=$(cat "$1")

regex="^\[(Python|TS|Config)\] .+"

if ! echo "$msg" | grep -Eq "$regex"; then
  echo "Invalid commit message."
  echo "Expected: [Python] ... | [TS] ... | [Config] ..."
  exit 1
fi